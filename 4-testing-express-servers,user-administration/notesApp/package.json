{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Bench Ham",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "build:ui_notes": "rm -rf dist/notes && cd ../2-communicating-with-server/notes && npm run build && cp -r dist ../4-testing-express-servers,user-administration/notesApp/dist",
    "build:full": "npm run build:ui_notes",
    "deploy:full": "npm run build:ui_notes && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^7.0.0",
    "mongoose": "^9.1.4",
    "morgan": "^1.10.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@stylistic/eslint-plugin": "^5.7.1",
    "eslint": "^9.39.2",
    "globals": "^17.2.0"
  }
}
