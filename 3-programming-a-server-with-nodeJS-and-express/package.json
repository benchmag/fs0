{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Bench Ham",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui_notes": "rm -rf dist_notes && cd ../2-communicating-with-server/notes && npm run build && cp -r dist ../../3-programming-a-server-with-nodeJS-and-express/dist/notes",
    "build:ui_pb": "rm -rf dist_pb && cd ../2-communicating-with-server/the-phonebook && npm run build && cp -r dist ../../3-programming-a-server-with-nodeJS-and-express/dist/phonebook",
    "deploy:full": "npm run build:ui_notes && npm run build:ui_pb && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1",
    "path": "^0.12.7"
  }
}
