```mermaid
sequenceDiagram
  participant Browser
  participant Server

  Browser->>Server: GET /exampleapp/spa
  activate Server
  Server-->>Browser: HTML Document
  deactivate Server

  Browser->>Server: GET /exampleapp/main.css
  activate Server
  Server-->>Browser: CSS Document
  deactivate Server

  Browser->>Server: GET /exampleapp/spa.js
  activate Server
  Server-->>Browser: JS Document
  deactivate Server

  Browser->>Server: GET /exampleapp/data.json
  activate Server
  Server-->>Browser: JSON object (data.json)
  deactivate Server

  Note left of Browser: Browser renders page using redrawNotes()

  Note left of Browser: User enters a new note and when they hit submit <br/> onsubmit() is triggered

  acivate Browser
  Note left of Browser: Whatever data is in the note is captured and put into a data variable called note <br/> which is pushed to the existing data.json and the form data field is reset to am empty string ""

  Note left of Browser:  Browser re-renders using redrawNotes()

  Browser->>Server: POST exampleapp/new_note_spa
  activate Server
  Note right of Server: new_note_spa data is added to the server
  Server-->>Browser: Sends data with a message saying the note was created
  deactivate Server
  
```
